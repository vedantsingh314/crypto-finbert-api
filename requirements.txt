# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1          # [standard] pulls websockets + httptools for speed
pydantic==2.9.2
pydantic-settings==2.6.1

# ── ML ────────────────────────────────────────────────────────────────────────
torch==2.4.1                       # CPU build — swap with torch+cu121 for GPU (see below)
transformers==4.46.1
sentencepiece==0.2.0               # Required by some tokenisers


# ── News / utils ──────────────────────────────────────────────────────────────
requests==2.32.3
python-dotenv==1.0.1

# ── Performance (optional but recommended) ────────────────────────────────────
# accelerate==1.0.1                # Faster model loading with device_map="auto"
# onnxruntime==1.19.2              # ONNX export for CPU inference: ~3× faster

huggingface_hub==0.26.2